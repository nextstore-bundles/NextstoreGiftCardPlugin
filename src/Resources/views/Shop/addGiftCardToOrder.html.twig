{% form_theme form '@SyliusShop/Form/theme.html.twig' %}

<div class="nextstore-sylius-gift-card-gift-card-block">
    <div class="ui hidden divider"></div>

    {{ form_start(form, {
        'action': path('nextstore_sylius_gift_card_shop_partial_add_gift_card_to_order'),
        'attr': {
            'id': 'nextstore-sylius-gift-card-add-gift-card-to-order',
            'class': 'ui loadable form',
            'novalidate': 'novalidate',
            'data-action': path('nextstore_sylius_gift_card_shop_ajax_add_gift_card_to_order'),
            'data-redirect': path('sylius_shop_cart_summary')
        }
    }) }}
    {{ form_row(form._token) }}
    {{ form_end(form, {'render_rest': false}) }}

    <div>
        <div class="ui action input">
            {{ form_widget(form.giftCard, {'attr': {'form': 'nextstore-sylius-gift-card-add-gift-card-to-order'}}) }}
            <button type="submit" class="ui primary icon labeled button" form="nextstore-sylius-gift-card-add-gift-card-to-order">
                <i class="ticket icon"></i> {{ 'nextstore_sylius_gift_card.ui.add_gift_card_to_order'|trans }}
            </button>
        </div>
        <br>
        {{ form_errors(form.giftCard) }}
    </div>
</div>
